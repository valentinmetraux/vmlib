# Bin headers
    # Check against text headers

# Trace headers
    # Get
        # NTrace (count, cdp_min, cdp_max)
        # Median fold
        # Max fold
    # Compare XY CDP/GRP/SRC
    # Compare Line_ID vs text-header
    # Check CDP sorting (dstance from cdp1)
    # CDP order (normal or reverse?)
    # Trace weighting factor
